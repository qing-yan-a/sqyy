# SQYY 医院管理系统 - 项目实现说明

## 一、项目概述

本项目为医院门诊管理系统初版，涵盖患者建档、挂号、就诊、处方、药房取药与注射等核心流程，支持多角色协同工作。

---

## 二、模块实现信息

### 2.1 后端模块

| 模块 | 包路径 | 职责 |
|------|--------|------|
| **认证** | `auth` | 登录、JWT 签发与校验、个人资料、修改密码 |
| **用户管理** | `admin` | 用户 CRUD、角色管理、药房/前台/科室配置 |
| **患者** | `patient` | 患者建档、信息维护、头像上传 |
| **就诊** | `visit` | 挂号、就诊记录创建/编辑/删除、待接诊列表、科室与医生列表 |
| **药房** | `pharmacy` | 待取药列表、待注射列表、收款、取药、完成注射 |
| **药品** | `drug` | 药品 CRUD、库存流水、库存调整 |
| **检索** | `search` | 关键词检索、患者检索、MySQL → ES 同步 |
| **时间轴** | `timeline` | 患者病历时间轴（就诊、诊断、处方） |
| **通用** | `common` | 文件上传、全局异常处理、`ApiResponse` 封装 |
| **配置** | `config` | 安全、JWT 过滤器、CORS、数据库迁移 |
| **持久化** | `persistence` | 实体与 MyBatis-Plus Mapper（MySQL） |
| **领域模型** | `model` | `HospitalModels` 中的领域对象定义 |

### 2.2 前端模块

| 模块 | 路径 | 职责 |
|------|------|------|
| **布局** | `layout/AppLayout.vue` | 顶部导航、侧边菜单、用户信息 |
| **路由** | `router/index.ts` | 路由定义、登录校验、角色守卫 |
| **认证** | `stores/auth.ts` | Token、用户信息、`hasRole` 判断 |
| **API** | `api/index.ts` | 封装所有后端接口调用 |
| **视图** | `views/*.vue` | 各业务页面 |

---

## 三、角色功能、职责与权限

### 3.1 角色一览

| 角色 | 中文 | 职责概述 |
|------|------|----------|
| **ADMIN** | 管理员 | 系统配置、用户管理、全流程查看与操作 |
| **RECEPTION** | 前台 | 患者建档、挂号、指定医生 |
| **DOCTOR** | 医生 | 接诊、开处方、编辑归属患者信息 |
| **PHARMACIST** | 药师 | 收款、取药、注射任务处理 |

### 3.2 各角色功能与权限

#### 管理员（ADMIN）

| 功能 | 说明 |
|------|------|
| 用户角色管理 | 增删用户、分配角色、部门/药房/前台配置 |
| 患者管理 | 查看、新增、编辑任意患者 |
| 就诊记录 | 查看、创建、编辑、删除就诊 |
| 药品管理 | 药品 CRUD、库存调整、库存流水 |
| 药房 | 待取药、待注射、收款、取药、完成注射（可处理所有任务） |
| 智能检索 | 关键词检索、手动触发 ES 同步 |
| 病历时间轴 | 查看患者完整病历时间轴 |
| 个人信息 | 查看与修改个人资料 |

#### 前台（RECEPTION）

| 功能 | 说明 |
|------|------|
| 患者信息 | 查看患者列表、新增患者 |
| 挂号服务 | 为患者挂号，选择科室与医生，生成排队号 |
| 就诊记录 | 查看就诊列表 |
| 智能检索 | 关键词检索 |
| 个人信息 | 查看与修改个人资料 |

**权限约束**：不能编辑患者信息，不能创建/编辑/删除就诊记录。

#### 医生（DOCTOR）

| 功能 | 说明 |
|------|------|
| 患者信息 | 查看患者、**仅编辑当前分配给自己（PENDING 挂号）的患者** |
| 就诊记录 | 查看、创建、编辑就诊（开处方） |
| 药品 | 查看药品列表（只读） |
| 病历时间轴 | 查看患者病历 |
| 个人信息 | 查看与修改个人资料 |

**权限约束**：只能编辑“前台已挂号给自己”的患者；删除就诊仅管理员可做。

#### 药师（PHARMACIST）

| 功能 | 说明 |
|------|------|
| 待取药 | 查看待取药列表，进入取药窗口 |
| 取药窗口 | 收款、取药（含注射药时，注射任务自动分配给当前药师） |
| 注射药物 | **仅查看并处理自己收款后分配的注射任务** |
| 药品管理 | 药品 CRUD、库存调整、库存流水 |
| 智能检索 | 关键词检索 |
| 个人信息 | 查看与修改个人资料 |

**权限约束**：注射待办仅显示“已收款且分配给自己”的任务；完成注射时校验负责人，非负责人不可操作。

### 3.3 接口级权限（@PreAuthorize）

| 接口 | 允许角色 |
|------|----------|
| 患者列表/新增 | ADMIN, DOCTOR, RECEPTION |
| 患者更新 | ADMIN, DOCTOR（后端再校验归属） |
| 挂号 | RECEPTION |
| 就诊创建/更新 | ADMIN, DOCTOR |
| 就诊删除 | ADMIN |
| 药房相关 | ADMIN, PHARMACIST |
| 药品列表 | ADMIN, DOCTOR, PHARMACIST |
| 药品增删、库存 | ADMIN, PHARMACIST |
| 用户管理 | ADMIN |
| 检索同步 | ADMIN |

---

## 四、前后端协同

### 4.1 认证流程

1. **登录**：`POST /api/auth/login` → 返回 `{ token, user }`
2. **Token 存储**：前端将 token 存入 `localStorage`（key: `sqyy-token`）
3. **请求携带**：`api/http.ts` 在请求头添加 `Authorization: Bearer <token>`
4. **后端校验**：`JwtAuthenticationFilter` 解析 JWT，将 `username` 与 `roles` 写入 `SecurityContext`
5. **个人资料**：`GET /api/auth/profile` 获取当前用户完整信息（含部门/药房/前台）

### 4.2 路由与权限

- **公开路由**：`/login`
- **需登录**：其余路由均需 `authStore.isLoggedIn`
- **角色守卫**：`meta.roles` 与 `authStore.hasRole(roles)` 匹配后才允许进入
- **菜单**：根据角色过滤，仅展示当前角色可访问的菜单项

### 4.3 接口映射（前端 → 后端）

| 前端 API | 后端路径 |
|----------|----------|
| `api.login` | POST `/auth/login` |
| `api.getProfile` | GET `/auth/profile` |
| `api.getPatients` / `createPatient` / `updatePatient` | GET/POST/PUT `/patients` |
| `api.getVisits` / `getPendingVisits` / `getDepartments` / `getDoctors` | GET `/visits/*` |
| `api.register` / `createVisit` / `updateVisit` / `deleteVisit` | POST/PUT/DELETE `/visits` |
| `api.getDrugs` / `searchDrugs` / `createDrug` / `deleteDrug` | GET/POST/DELETE `/drugs` |
| `api.getInventoryLogs` / `adjustStock` | GET/POST `/drugs/inventory/logs` |
| `api.getPickupTodos` / `getInjectionTodos` / `getPharmacyVisitDetail` | GET `/pharmacy/*` |
| `api.confirmPayment` / `confirmPickup` / `completeInjection` | POST `/pharmacy/visits/{id}/*` |
| `api.getAdminUsers` / `createUser` / `deleteUser` | GET/POST/DELETE `/admin/users` |
| `api.search` / `searchPatients` / `syncSearch` | GET/POST `/search/*` |
| `api.getTimeline` | GET `/timeline/patients/{id}` |

### 4.4 代理配置

前端开发时通过 Vite 代理将 `/api` 转发至后端，生产环境由 Nginx 等反向代理统一转发。

---

## 五、MySQL 与 Elasticsearch 同步

### 5.1 同步策略

- **主数据源**：MySQL（MyBatis-Plus）
- **检索数据源**：Elasticsearch（可选，可降级为内存检索）
- **同步方向**：MySQL → ES，单向写入

### 5.2 同步触发时机

1. **应用启动**：`app.search.auto-sync-on-startup: true` 时，启动完成后执行一次全量同步
2. **手动同步**：`POST /api/search/sync`（仅 ADMIN）
3. **数据变更后**：
   - 患者：创建、更新
   - 就诊：挂号、创建、更新、删除
   - 药品：创建、删除、库存调整
   - 药房：收款、取药、完成注射

### 5.3 索引结构

| 索引 | 数据来源 | 主要字段 |
|------|----------|----------|
| `{prefix}-patient` | 患者 | id, name, patientNo, phone, idCard, address, 过敏史, 既往病史 |
| `{prefix}-visit` | 就诊详情 | 就诊 id、患者名、科室、主诉、医生、诊断、处方 |
| `{prefix}-drug` | 药品 | id, drugName, drugCode, 规格, 厂商, 单位, 库存 |

检索使用 `multiMatch`，字段权重：`title^3`、`subtitle^2`、`content`、`tags`。

### 5.4 降级机制

- ES 不可用时，`ElasticsearchSearchService` 捕获异常，自动回退到 `HospitalPersistenceService.search()` 的内存检索
- 配置 `app.search.engine: memory` 时使用 `MemorySearchService`，`syncAll()` 为空实现

---

## 六、并发问题与解决方案

### 6.1 事务

所有写操作均使用 `@Transactional`，保证单次请求内多表操作原子性。

### 6.2 悲观锁（SELECT FOR UPDATE）

| 场景 | 实现 | 作用 |
|------|------|------|
| **药品库存扣减** | `DrugInfoMapper.selectByIdForUpdate(drugId)` | 扣减前锁定药品行，避免超卖 |
| **药房操作** | `VisitRecordMapper.selectByIdForUpdate(visitId)` | 收款/取药/注射前锁定就诊记录，同一就诊串行处理 |

### 6.3 乐观校验

| 场景 | 实现 | 作用 |
|------|------|------|
| **重复取药** | 若所有处方已 `PICKUP_DISPENSED` | 返回 409，提示“该取药事务已被其他药师处理，请刷新页面” |
| **重复完成注射** | 若所有注射处方已 `INJECTION_COMPLETED` | 返回 409，同上 |

### 6.4 注射任务分配

- **分配时机**：药师确认收款时，对该就诊下所有注射类处方写入 `injection_assignee_username = 当前药师`
- **列表过滤**：非管理员药师仅能看到 `injection_assignee_username = 自己` 的待注射任务
- **完成校验**：非管理员完成注射时，校验当前用户是否为负责人，否则返回 403

### 6.5 患者编辑权限

- **校验逻辑**：医生编辑患者时，后端检查是否存在 `patientId` + `status=PENDING` + `doctorName=当前医生 realName` 的挂号记录
- **无匹配**：返回 403，“该患者当前未分配给您，无法编辑”

### 6.6 典型并发流程（取药）

1. `lockVisitRecord(visitId)` 锁定就诊
2. 查询该就诊下所有处方
3. 校验收款状态、取药状态
4. 若已全部取药 → 409
5. 逐条扣减库存（`selectByIdForUpdate` 锁药品行）
6. 更新处方状态
7. 提交事务

---

## 七、技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3、Vite、Element Plus、Vue Router、Pinia |
| 后端 | Spring Boot、Spring Security、JWT |
| 持久化 | MyBatis-Plus、MySQL |
| 检索 | Elasticsearch（可选） |
| 构建 | Maven、npm |

---

## 八、配置要点

- **数据库**：`application.yml` 中配置 MySQL 连接
- **JWT**：`app.auth.secret`、`app.auth.expire-seconds`
- **检索**：`app.search.engine`、`app.search.auto-sync-on-startup`、ES 连接信息
- **科室/药房/前台**：`app.departments`、`app.pharmacy`、`app.reception` 可覆盖默认列表
